# Геометрические фигуры: вычисление периметра и площади

''' Этот скрипт позволяет вычислять периметр и площадь для двух фигур: круга и квадрата. Пользователь вводит имя фигуры, нужную функцию и размеры, после чего программа производит необходимые вычисления.
'''
## Импортируемые модули
'''
import circle
import square
'''

## Доступные фигуры и функции

' **figs**:'
''' список доступных фигур:'''
- 'circle'
- 'square'

'**funcs**:'
''' список доступных функций:'''
- 'perimeter'
- 'area'

## Глобальные переменные
```
figs = ['circle', 'square']
funcs = ['perimeter', 'area']

sizes = {}
```

''' size — хранит количество параметров для каждой комбинации функции и фигуры.
'''
## Функция calc
```
def calc(fig, func, size):
	assert fig in figs
	assert func in funcs

	result = eval(f'{fig}.{func}(*{size})')
	print(f'{func} of {fig} is {result}')
```
### Параметры:
'''- fig: строка, имя фигуры (например, 'circle' или 'square').
- func: строка, имя функции (например, 'perimeter' или 'area').
- size: список, размеры фигуры.'''

### Описание:
'''Функция проверяет, что фигура и функция допустимы, затем использует eval() для вызова соответствующей функции из импортированного модуля с переданными размерами. Результат выводится на экран.
'''
## Основной блок программы
```
if __name__ == "__main__":
	func = ''
	fig = ''
	size = list()
    
	while fig not in figs:
		fig = input(f"Enter figure name, avaliable are {figs}:\n")
	
	while func not in funcs:
		func = input(f"Enter function name, avaliable are {funcs}:\n")
	
	while len(size) != sizes.get(f"{func}-{fig}", 1):
		size = list(map(int, input("Input figure sizes separated by space, 1 for circle and square\n").split(' ')))
	
	calc(fig, func, size)
```
### Описание:
'''1. **Ввод фигуры**: Программа запрашивает у пользователя ввести имя фигуры, пока не будет введено допустимое значение.
2. **Ввод функции**: Аналогично, программа запрашивает функцию.
3. **Ввод размеров**: Программа запрашивает размеры фигуры, пока не будет введено нужное количество параметров.
4. **Вызов функции**: После получения всех данных программа вызывает функцию calc для вычисления результата.
'''
## Примечание
'''- Убедитесь, что модули circle и square содержат соответствующие функции для вычисления периметра и площади.
- Использование eval() может быть небезопасным; следует убедиться, что входные данные контролируются и валидируются.
'''


